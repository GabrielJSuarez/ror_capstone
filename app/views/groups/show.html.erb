<%= render 'shared/navbar' %>

<div class="text-center fs-2 py-1 bg-primary text-light">
  <p class="mb-0 proxima-bold">
    <%= @group.name.capitalize %> Projects
  </p>
</div>

<div class="mb-feed">
  <%= display_group_projects(@group.projects) %>
</div>

<div class="fixed-bottom">
  <div class="d-grid gap-2 p-3 bg-success text-white proxima-light text-muted">
    <%= link_to 'Add Project', new_log_path, class: 'btn btn-outline-light py-2 w-100 proxima-light' %>
  </div>
</div>