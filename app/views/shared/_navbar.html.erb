<div class="p-3 pb-0 bg-primary text-white">
  <nav class="navbar navbar-expand-lg navbar navbar-dark bg-primary">
    <div class="container-fluid">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <%= link_to request.referer.present? ? request.referer : authenticated_root_path do %>
        <i class="fas fa-angle-double-left text-white fs-1"></i>
      <% end %>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item proxima-bold">
            <%= user_name %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle proxima-bold" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Menu
            </a>
            <ul class="dropdown-menu proxima-bold" aria-labelledby="navbarDropdown">
              <%= navbar_dropdown %>
            </ul>
          </li>
          <% if signed_in? %>
            <li class="nav-item proxima-bold">
              <%= link_to 'Log Out', destroy_user_session_path, method: :delete, class: 'nav-link proxima-bold' %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</div>
