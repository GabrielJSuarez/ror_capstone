<%= render 'shared/navbar' %>

<div class="px-2">
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs pt-2">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" },
                  label: false,
                  placeholder: 'Email', html_class: 'border-0',
                  input_html: { class: "border-0 mb-2" } %>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" },
                  label: false,
                  placeholder: 'Password',
                  input_html: { class: "border-0 mb-2" } %>
    </div>

    <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
      <%= link_to "Forgot your password?", new_password_path(resource_name), class: 'text-center nav-link text-muted' %><br />
    <% end %>

    <div class="fixed-bottom">
      <div class="d-flex justify-content-center text-muted pb-2">
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
      </div>

      <div class="d-grid gap-2 p-3 bg-primary text-white">
        <%= f.button :submit, "Log in", class: 'btn btn-outline-light py-2 w-100' %>
      </div>
    </div>
  <% end %>
</div>
